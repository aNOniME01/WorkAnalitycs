<Page x:Class="WorkAnalitycsWPF.View.ViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WorkAnalitycsWPF.View"
      mc:Ignorable="d" 
      d:DesignHeight="20" d:DesignWidth="800"
      Title="ViewPage" Loaded="Page_Loaded">

    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" x:Name="nameText" FontSize="16"/>
        <TextBlock Text="Delivery" Grid.Column="1" x:Name="expectedDelivery" FontSize="16"/>
        <TextBlock Text="Delivery" Grid.Column="2" x:Name="deliveryDate" FontSize="16"/>
        <TextBlock Text="workHours" Grid.Column="3" x:Name="workHours" FontSize="16"/>
        <TextBlock Text="Price" Grid.Column="4" x:Name="price" FontSize="16"/>
        <Button Grid.Column="6" Content="File Location"/>

    </Grid>
</Page>
